// Placeholder screen for the center "+" tab. Its tabPress listener in
// _layout.tsx intercepts the press and opens today's entry editor instead,
// so this never actually renders.
export default function NewEntryTab() {
  return null;
}
